# third_party

Vendored upstream source trees live here.

Current submodules:

- `third_party/verilator` at `v5.046`
- `third_party/cocotb` at `v2.0.1`

Suggested policies:

- pin explicit upstream versions
- record the upstream commit or tag in your commit message or README
- avoid tracking generated build output inside these trees
